import { Typography } from "@mui/material";
import { NavBar } from "../components/navbar";

export const CartPage = () => {
    return (
        <div>
            <NavBar cart/>
        </div>
    );
};